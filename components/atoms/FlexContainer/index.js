import React from "react";
import * as S from "./styled";

const FlexContainer = ({ children }) => {
  return <S.StyledFlexContainer>{children}</S.StyledFlexContainer>;
};

export default FlexContainer;
