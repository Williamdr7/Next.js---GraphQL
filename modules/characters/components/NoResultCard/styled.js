import { StyledFlexContainer } from "@atoms/FlexContainer/styled";
import styled from "styled-components";

export const NoResultContainer = styled(StyledFlexContainer)`
  width: 100%;
  & > span {
    margin: auto !important;
  }
`;
